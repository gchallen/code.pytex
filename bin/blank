#!/usr/bin/env python

import sys,subprocess
subprocess.check_output("convert -size %dx%d -density 300 -format pdf xc:white %s" % (int(int(sys.argv[2]) * 300),
                                                                                      int(int(sys.argv[3]) * 300),
                                                                                      sys.argv[1]), shell=True)
